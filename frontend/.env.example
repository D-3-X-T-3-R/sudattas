# GraphQL API (Create React App embeds REACT_APP_* at build time)
# Default: http://localhost:8080/v2
REACT_APP_GRAPHQL_URL=

# Optional: fixed token or session for dev (overrides guest session / Auth0 when set)
# REACT_APP_GRAPHQL_TOKEN=
# REACT_APP_GRAPHQL_SESSION_ID=

# Auth0 (optional — when set, login/logout appear and JWT is sent for placeOrder/searchOrder)
# REACT_APP_AUTH0_DOMAIN=your-tenant.auth0.com
# REACT_APP_AUTH0_CLIENT_ID=your_client_id
# REACT_APP_AUTH0_AUDIENCE=           # Match backend OAUTH_AUDIENCE
# REACT_APP_AUTH0_REDIRECT_URI=       # Default: window.location.origin
